test:
  driver: postgres
  open: dbname=auth_service_test sslmode=disable
development:
  driver: postgres
  open: dbname=auth_service_development sslmode=disable
production:
  driver: postgres
  open: $DATABASE_URL
