syntax = "proto3";

package auth;

service Auth {
  rpc VerifyToken(Args) returns (Customer) {}
}

message Args {
  string traceId = 1;
  string from = 2;
  string authToken = 3;
}

message Customer {
  int32 id = 1;
  string authToken = 2;
}
